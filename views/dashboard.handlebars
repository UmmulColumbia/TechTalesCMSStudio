<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <link rel="stylesheet" href="/style.css"> <!-- Adjust the path as needed -->
</head>
<body>
    <header>
        <h1>Welcome to Your Application Dashboard</h1>
        <nav>
            <a href="/">Home</a>
            <a href="/logout">Logout</a>
        </nav>
    </header>

    <main class="container">
        <h1>Your Dashboard</h1>
        {{#if posts.length}}
        <ul>
            {{#each posts}}
            <li><a href="/posts/edit/{{this.id}}">{{this.title}}</a></li>
            {{/each}}
        </ul>
        {{else}}
        <p>You have no posts yet.</p>
        {{/if}}

        <a href="/posts/new">Create New Post</a>
    </main>

    <footer>
        <p>Copyright &copy; Your Company, Year</p>
    </footer>
</body>
</html>
