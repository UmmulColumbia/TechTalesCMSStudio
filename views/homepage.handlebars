<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Homepage</title>
</head>
<body>
    {{#each posts}}
        <div>
            <h2>{{this.title}}</h2>
            <p>{{this.content}}</p>
        </div>
    {{/each}}
</body>
</html>
